This foloder is a prototype of programs using logics,
such as AND and OR, in order to understand algorithms,
and to check if the algorithms work appropriately.

Any logics can be run by typing following command.

	$ python main.py